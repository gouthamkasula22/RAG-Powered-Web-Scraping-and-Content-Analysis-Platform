# Docker-specific environment variables
# This file is used by the Docker containers

# Environment
ENVIRONMENT=production

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_DEBUG=false
API_WORKERS=4
API_LOG_LEVEL=info

# Streamlit Configuration
STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_ADDRESS=0.0.0.0
STREAMLIT_BROWSER_SERVER_ADDRESS=localhost
STREAMLIT_SERVER_HEADLESS=true

# Backend URL for frontend to connect to
# In Docker, we use the service name as hostname
BACKEND_URL=http://backend:8000

# Scraping Configuration
SCRAPING_TIMEOUT=30
SCRAPING_MAX_RETRIES=3
SCRAPING_DELAY_BETWEEN_REQUESTS=1
SCRAPING_MAX_CONTENT_SIZE=1000000

# Security Configuration
ALLOWED_ORIGINS=http://localhost:8501,http://frontend:8501

# Database Configuration
DATABASE_PATH=/app/data

# LLM Services (replace with your own API keys)
GOOGLE_API_KEY=your-google-api-key
ANTHROPIC_API_KEY=your-anthropic-api-key

# Python paths
PYTHONPATH=/app
